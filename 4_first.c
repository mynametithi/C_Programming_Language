 //Calculate the area of circle and modify the same program to calculatethe volume of a cylinder given its radius and heighrt.
 #include<stdio.h>
 int main(){
int radius=4;
float pi=3.14;
printf("The area of this circle is %f \n",pi*radius*radius);
int height=12;
printf("Volume of this cylinder is %f \n",pi*radius*radius*height);
    return 0;
 }
